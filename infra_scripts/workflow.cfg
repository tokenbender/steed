# Legacy compatibility shim.
#
# New canonical config layout uses profile files under:
#   infra_scripts/workflow/<profile>.cfg
#
# This shim keeps older entrypoints working by sourcing the default profile.

# shellcheck disable=SC1091
source "$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)/workflow/default.cfg"
